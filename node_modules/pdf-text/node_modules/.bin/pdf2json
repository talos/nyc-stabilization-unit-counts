#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pdf3json/bin/pdf2json" "$@"
  ret=$?
else 
  node  "$basedir/../pdf3json/bin/pdf2json" "$@"
  ret=$?
fi
exit $ret
